/** Names of components that are searched for in HTML attributes. */
export enum ComponentName {
    Includer = 'includer',
    BackToTopButton = 'back-to-top-button',
    SearchForm = 'search-form',
    ModTextSearcher = 'mod-text-searcher',
}
